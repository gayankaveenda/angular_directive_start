import { HighlightHostListenerDirective } from './highlight-host-listener.directive';

describe('HighlightHostListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightHostListenerDirective();
    expect(directive).toBeTruthy();
  });
});
